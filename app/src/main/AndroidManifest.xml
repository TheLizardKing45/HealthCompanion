<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".activity.mental.MentalHealthMoodActivity"></activity>
        <activity android:name=".activity.body.BodyHealthWeightActivity" />
        <activity android:name=".activity.AddGoalActivity" />
        <activity android:name=".activity.body.BodyHealthBloodActivity" />
        <activity android:name=".activity.body.BodyHealthExerciseActivity" />
        <activity android:name=".activity.mental.MentalMenuActivity" />
        <activity android:name=".activity.body.BodyMenuActivity" />
        <activity android:name=".activity.GoalsMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>



</manifest>